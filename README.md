# The Mosfet Programming Language

This repository contains the compiler, standard library, and documentation of the Mosfet language.

## Building

To build the compiler use the command `cargo build`.

## Testing

To run all the tests use the command `cargo test`.
